# 桌面端专用配置 - 强制使用 npm 风格的扁平化物理结构
# 解决 Electron 打包器对 pnpm 符号链接支持不佳的问题

# 使用 hoisted 链接器（创建扁平且物理真实的 node_modules）
node-linker=hoisted

# 禁用所有符号链接，强制复制物理文件
symlink=false

# 不使用严格的对等依赖检查
strict-peer-dependencies=false

# 自动安装对等依赖
auto-install-peers=true

# 确保原生模块在安装时重建
rebuild-on-install=true

# 强制安装所有可选依赖
optional-dependencies=true
supported-architectures=win32-x64,darwin-x64,darwin-arm64,linux-x64

# 提高并发以加快物理复制过程
child-concurrency=20
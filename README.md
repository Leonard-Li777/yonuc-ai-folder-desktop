# Yonuc AI Folder (Desktop App)

**中文** | [English](README_EN.md)

**智能虚拟文件管理工具** 是一个基于 AI 的文件管理系统，旨在解决用户文件散乱、命名混乱、多维度归类困难等问题。该工具特别针对压缩文件（如漫画压缩包）的管理，提供智能分类、密码管理、多维度查看和一键整理功能。

## ✨ 核心功能

*   **AI 智能分类**: 集成 Ollama 本地大模型，自动分析文件内容并进行智能归类。
*   **虚拟目录系统**: 无需改变物理文件位置，通过虚拟目录实现多维度文件管理。
*   **[TODO]压缩包管理**: 深度支持 ZIP/RAR 等压缩格式，支持密码管理和内容预览。
*   **多语言支持**: 内置多语言系统（VoerkaI18n），支持中、英、日等多国语言。
*   **文件系统监控**: 实时监控文件变化，保持虚拟目录与物理文件同步。
*   **本地优先**: 所有数据和 AI 处理均在本地运行，保护用户隐私。

## 🛠 技术栈

*   **Core**: [Electron](https://www.electronjs.org/), [Electron Vite](https://electron-vite.org/)
*   **Frontend**: [React](https://react.dev/), [TypeScript](https://www.typescriptlang.org/), [Tailwind CSS](https://tailwindcss.com/)
*   **UI Components**: [Radix UI](https://www.radix-ui.com/)
*   **Database**: [Better-SQLite3](https://github.com/WiseLibs/better-sqlite3), [Supabase](https://supabase.com/) (Sync)
*   **AI Integration**: [Ollama](https://ollama.com/), LlamaIndex
*   **I18n**: [VoerkaI18n](https://github.com/voerkai18n/voerkai18n)

## 🚀 快速开始

### 前置要求

*   Node.js (建议 v18+)
*   pnpm (包管理工具)
*   [Ollama](https://ollama.com/) (用于 AI 功能支持)

### 安装依赖

```bash
pnpm install
```

### 开发环境运行

启动开发服务器（包含主进程和渲染进程）：

```bash
pnpm dev
```

#### 其他启动模式

*   **仅 AI 服务**: `pnpm start:ai-only` (仅启动 AI 相关日志和服务)
*   **分析模式**: `pnpm start:analysis` (重点关注文件分析和队列日志)
*   **调试模式**: `pnpm start:debug` (开启详细日志)
*   **安静模式**: `pnpm start:quiet` (仅显示错误日志)

### 构建与打包

构建生产版本：

```bash
pnpm build
```

打包应用程序（生成安装包）：

```bash
pnpm package
```

生成安装程序：

```bash
pnpm make
```

## 📂 项目结构

```
apps/desktop/
├── src/
│   ├── electron/          # Electron 主进程代码
│   │   ├── adapters/      # 适配器层
│   │   ├── config/        # 配置管理
│   │   ├── runtime-services/ # 运行时服务 (AI, Database, Filesystem 等)
│   │   └── main.ts        # 主进程入口
│   ├── renderer/          # React 渲染进程代码 (UI)
│   ├── languages/         # 国际化翻译文件
│   └── shared/            # 前后端共享类型和工具
├── scripts/               # 构建和维护脚本
├── build/                 # 构建资源 (图标, 额外配置等)
└── electron.vite.config.mts # Electron-Vite 配置文件
```

确保已安装 Ollama 并下载了所需的模型（如`qwen3` 等，具体参考配置文件）。

## � 联系方式

*   作者邮箱：seaeye777@qq.com

## �📄 许可证

[CC BY-NC-SA 4.0 License](LICENSE)
